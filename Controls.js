// a set of controls for user input
class Controls extends Item {
  constructor(params) {
    super(params);
    this.type = "Controls";
  }

  setup() {
    // add child Items that are Buttons
  }

  addChild() {
    // add child overlay controls

  }
  
}